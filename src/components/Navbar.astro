---
import TPLogo from "../assets/logo.png";
import Dropdown from "./Dropdown.astro";
import { Picture } from "astro:assets";
import { Astronav, MenuItems, MenuIcon } from "astro-navbar";
// oconst navItems = [
//   {
//     title: "Experiencia",
//     label: "experiencia",
//     url: "/#experiencia",
//   },
//   {
//     title: "Proyectos",
//     label: "proyectos",
//     url: "/#proyectos",
//   },
//   {
//     title: "Sobre mí",
//     label: "sobre-mi",
//     url: "/#sobre-mi",
//   },
//   {
//     title: "Contacto",
//     label: "contacto",
//     url: "mailto:miduga@gmail.com",
//   },
// ]

const menuitems = [
    {
        title: "Pricing",
        url: "#pricing",
    },
    {
        title: "Timetable",
        path: "/timetable",
    },
    {
        title: "Coaches",
        path: "/coaches",
    },
    {
        title: "Contact",
        path: "/contact",
    },
];
---

<header
    class="p-4 h-13 flex flex-col lg:flex-row justify-between items-center bg-black text-white bg-opacity-0"
>
    <Astronav>
        <div class="flex w-full lg:w-auto items-center justify-between">
            <a href="/">
                <Picture
                    src={TPLogo}
                    alt="TP Phuket Academy"
                    class="w-15 h-15 md:w-20 md:h-20"
                />
            </a>
            <div class="block lg:hidden">
                <MenuIcon class="w-4 h-4 text-white" />
            </div>
        </div>
        <MenuItems
            class="hidden w-full lg:w-auto mt-2 lg:flex lg:mt-0 text-2xl font-bold"
        >
            <ul
                class="flex flex-col lg:flex-row lg:gap-3 justify-center items-center"
            >
                {
                    menuitems.map((item, index) => (
                        <>
                            {item.children && (
                                <Dropdown
                                    title={item.title}
                                    children={item.children}
                                    lastItem={index === menuitems.length - 1}
                                />
                            )}

                            {!item.children && (
                                <li>
                                    <a
                                        href={item.url}
                                        class="flex lg:px-3 py-2 items-center text-white hover:text-gray-900"
                                    >
                                        <span> {item.title}</span>
                                        {item.badge && (
                                            <span class="ml-1 px-2 py-0.5 text-[10px] animate-pulse font-semibold uppercase text-white bg-indigo-600 rounded-full">
                                                New
                                            </span>
                                        )}
                                    </a>
                                </li>
                            )}
                        </>
                    ))
                }
            </ul>
        </MenuItems>
    </Astronav>
    <div></div>
</header>
